version: "3.8"

services:
  mensabot:
    build: .
    container_name: mensabot
    environment:
      - MENSABOT_TOKEN=${MENSABOT_TOKEN}
      - TZ=Europe/Berlin
    volumes:
      - mensabot_data:/app/db
    restart: unless-stopped

volumes:
  mensabot_data:
